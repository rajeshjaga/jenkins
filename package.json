{
  "name": "jenkins-demo-project",
  "version": "1.0.0",
  "description": "Complete Jenkins CI/CD demonstration project",
  "main": "app.js",
  "scripts": {
    "start": "node app.js",
    "dev": "nodemon app.js",
    "test": "jest --verbose --coverage",
    "test:watch": "jest --watch",
    "lint": "eslint *.js",
    "lint:fix": "eslint *.js --fix",
    "build": "echo 'Build process completed successfully' && npm run lint",
    "prebuild": "echo 'Running pre-build checks...'",
    "postbuild": "echo 'Post-build tasks completed'"
  },
  "dependencies": {
    "express": "^4.18.2"
  },
  "devDependencies": {
    "jest": "^29.5.0",
    "supertest": "^6.3.3",
    "nodemon": "^3.0.1",
    "eslint": "^8.42.0"
  },
  "engines": {
    "node": ">=14.0.0",
    "npm": ">=6.0.0"
  },
  "keywords": ["jenkins", "ci-cd", "demo", "nodejs", "express"],
  "author": "Demo User",
  "license": "MIT"
}
